# Costco Same-Day Receipts

Place your Costco Same-Day order receipt PDFs in this folder.
The parser will automatically parse them when you run the import scripts.

*Note: For privacy, all PDF files in this directory are gitignored so they are never committed to your repository.*
