# Walmart Order Exports

Place your Walmart order Excel exports (`.xlsx`) in this folder.
The parser will automatically parse them when you run the import scripts.

*Note: For privacy, all spreadsheet files in this directory are gitignored so they are never committed to your repository.*
